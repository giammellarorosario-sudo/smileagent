<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Messaggi - SmileAgent</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            primary: '#0ea5e9',
          }
        }
      }
    }
  </script>
</head>
<body class="bg-gray-50 dark:bg-gray-800">
  <div class="flex min-h-screen">
    <%- include('../partials/sidebar', { currentPage, studioNome }) %>

    <main class="flex-1 p-8">
      <div class="max-w-7xl mx-auto">
        <!-- Header -->
        <div class="flex items-center justify-between mb-6">
          <h1 class="text-3xl font-bold text-gray-900 dark:text-white">📬 Messaggi Unificati</h1>

          <!-- Toggle AI Attiva -->
          <div class="flex items-center gap-4">
            <div class="flex items-center gap-2 bg-white dark:bg-gray-900 px-4 py-2 rounded-lg border border-gray-200 dark:border-gray-700">
              <span class="text-sm font-medium text-gray-700 dark:text-gray-300">AI Auto-Risposta</span>
              <label class="relative inline-flex items-center cursor-pointer">
                <input type="checkbox" id="aiToggle" class="sr-only peer" onchange="toggleAI(this.checked)">
                <div class="w-11 h-6 bg-gray-200 peer-focus:outline-none peer-focus:ring-4 peer-focus:ring-primary/20 rounded-full peer dark:bg-gray-700 peer-checked:after:translate-x-full after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-green-600"></div>
              </label>
              <span id="aiStatus" class="text-xs font-semibold text-gray-500">OFF</span>
            </div>
          </div>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
          <!-- LISTA MESSAGGI -->
          <div class="lg:col-span-2">
            <!-- Filtri -->
            <div class="bg-white dark:bg-gray-900 rounded-lg border border-gray-200 dark:border-gray-700 p-4 mb-4">
              <div class="flex gap-2 overflow-x-auto">
                <button onclick="filterMessages('all')" class="filter-btn active px-4 py-2 rounded-lg text-sm font-medium whitespace-nowrap">
                  Tutti
                </button>
                <button onclick="filterMessages('gmail')" class="filter-btn px-4 py-2 rounded-lg text-sm font-medium whitespace-nowrap">
                  📧 Gmail
                </button>
                <button onclick="filterMessages('facebook')" class="filter-btn px-4 py-2 rounded-lg text-sm font-medium whitespace-nowrap">
                  📘 Facebook <span class="text-xs ml-1 opacity-60">(Demo)</span>
                </button>
                <button onclick="filterMessages('instagram')" class="filter-btn px-4 py-2 rounded-lg text-sm font-medium whitespace-nowrap">
                  📷 Instagram <span class="text-xs ml-1 opacity-60">(Demo)</span>
                </button>
              </div>
            </div>

            <!-- Messaggi Gmail -->
            <div id="messagesList" class="space-y-3">
              <!-- Connetti Gmail -->
              <div id="gmailConnect" class="bg-blue-50 dark:bg-blue-900/20 rounded-lg border-2 border-dashed border-blue-300 dark:border-blue-700 p-6 text-center">
                <div class="inline-flex items-center justify-center w-16 h-16 bg-blue-100 dark:bg-blue-800 rounded-full mb-4">
                  <span class="material-symbols-outlined text-blue-600 dark:text-blue-400 text-3xl">mail</span>
                </div>
                <h3 class="text-lg font-bold text-gray-900 dark:text-white mb-2">Connetti Gmail</h3>
                <p class="text-sm text-gray-600 dark:text-gray-400 mb-4">Collega smileagent.italia@gmail.com per gestire email pazienti con AI</p>
                <button onclick="connectGmail()" class="bg-blue-600 text-white px-6 py-2 rounded-lg hover:bg-blue-700 font-semibold">
                  <span class="material-symbols-outlined align-middle text-sm mr-1">link</span>
                  Connetti Gmail
                </button>
              </div>

              <!-- Messaggi Demo Facebook/Instagram (FAKE) -->
              <div class="message-item facebook-msg bg-white dark:bg-gray-900 rounded-lg border border-gray-200 dark:border-gray-700 p-4 hover:shadow-md transition-shadow cursor-pointer" onclick="openConversation('facebook', 'fb_demo_001')">
                <div class="flex items-start gap-3">
                  <div class="w-10 h-10 bg-blue-600 rounded-full flex items-center justify-center flex-shrink-0">
                    <span class="text-white font-bold">FB</span>
                  </div>
                  <div class="flex-1 min-w-0">
                    <div class="flex items-center justify-between mb-1">
                      <div class="flex items-center gap-2">
                        <span class="font-semibold text-gray-900 dark:text-white">Laura Verdi</span>
                        <span class="text-xs bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300 px-2 py-0.5 rounded">📘 Facebook</span>
                        <span class="text-xs bg-yellow-100 dark:bg-yellow-900 text-yellow-700 dark:text-yellow-300 px-2 py-0.5 rounded">DEMO</span>
                      </div>
                      <span class="text-xs text-gray-500">5 ore fa</span>
                    </div>
                    <p class="text-sm text-gray-600 dark:text-gray-400 truncate">Quanto costa l'apparecchio invisibile? Mio figlio ne avrebbe bisogno...</p>
                  </div>
                </div>
              </div>

              <div class="message-item instagram-msg bg-white dark:bg-gray-900 rounded-lg border border-gray-200 dark:border-gray-700 p-4 hover:shadow-md transition-shadow cursor-pointer" onclick="openConversation('instagram', 'ig_demo_001')">
                <div class="flex items-start gap-3">
                  <div class="w-10 h-10 bg-gradient-to-br from-purple-600 to-pink-600 rounded-full flex items-center justify-center flex-shrink-0">
                    <span class="text-white font-bold">IG</span>
                  </div>
                  <div class="flex-1 min-w-0">
                    <div class="flex items-center justify-between mb-1">
                      <div class="flex items-center gap-2">
                        <span class="font-semibold text-gray-900 dark:text-white">@dentista_fan</span>
                        <span class="text-xs bg-purple-100 dark:bg-purple-900 text-purple-700 dark:text-purple-300 px-2 py-0.5 rounded">📷 Instagram</span>
                        <span class="text-xs bg-yellow-100 dark:bg-yellow-900 text-yellow-700 dark:text-yellow-300 px-2 py-0.5 rounded">DEMO</span>
                      </div>
                      <span class="text-xs text-gray-500">1 giorno fa</span>
                    </div>
                    <p class="text-sm text-gray-600 dark:text-gray-400 truncate">Complimenti per il post sullo sbiancamento! Fate anche consulenze gratuite?</p>
                  </div>
                </div>
              </div>

              <div class="message-item facebook-msg bg-white dark:bg-gray-900 rounded-lg border border-gray-200 dark:border-gray-700 p-4 hover:shadow-md transition-shadow cursor-pointer" onclick="openConversation('facebook', 'fb_demo_002')">
                <div class="flex items-start gap-3">
                  <div class="w-10 h-10 bg-blue-600 rounded-full flex items-center justify-center flex-shrink-0">
                    <span class="text-white font-bold">FB</span>
                  </div>
                  <div class="flex-1 min-w-0">
                    <div class="flex items-center justify-between mb-1">
                      <div class="flex items-center gap-2">
                        <span class="font-semibold text-gray-900 dark:text-white">Marco Bianchi</span>
                        <span class="text-xs bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300 px-2 py-0.5 rounded">📘 Facebook</span>
                        <span class="text-xs bg-yellow-100 dark:bg-yellow-900 text-yellow-700 dark:text-yellow-300 px-2 py-0.5 rounded">DEMO</span>
                      </div>
                      <span class="text-xs text-gray-500">2 giorni fa</span>
                    </div>
                    <p class="text-sm text-gray-600 dark:text-gray-400 truncate">Salve, vorrei prenotare una pulizia dentale. Quali sono i vostri orari?</p>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- CONVERSAZIONE -->
          <div class="lg:col-span-1">
            <div class="bg-white dark:bg-gray-900 rounded-lg border border-gray-200 dark:border-gray-700 p-6 sticky top-8">
              <div id="conversationPlaceholder" class="text-center py-12">
                <span class="material-symbols-outlined text-gray-400 text-6xl">forum</span>
                <p class="text-sm text-gray-500 mt-3">Seleziona un messaggio per visualizzare la conversazione</p>
              </div>

              <div id="conversationView" class="hidden">
                <div class="flex items-center gap-3 pb-4 border-b border-gray-200 dark:border-gray-700 mb-4">
                  <div id="convAvatar" class="w-12 h-12 bg-blue-600 rounded-full flex items-center justify-center">
                    <span class="text-white font-bold">?</span>
                  </div>
                  <div>
                    <h3 id="convName" class="font-bold text-gray-900 dark:text-white">Nome Contatto</h3>
                    <p id="convChannel" class="text-xs text-gray-500">Canale</p>
                  </div>
                </div>

                <div id="convMessages" class="space-y-3 mb-4 max-h-96 overflow-y-auto">
                  <!-- Messages populated by JS -->
                </div>

                <div class="mt-4">
                  <textarea id="replyText" rows="3" placeholder="Scrivi una risposta (o attiva AI)..." class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-primary dark:bg-gray-800 dark:text-white resize-none"></textarea>
                  <div class="flex gap-2 mt-2">
                    <button onclick="generateAIReply()" class="flex-1 bg-purple-600 text-white px-4 py-2 rounded-lg hover:bg-purple-700 text-sm font-semibold">
                      <span class="material-symbols-outlined align-middle text-sm mr-1">auto_awesome</span>
                      AI Risposta
                    </button>
                    <button onclick="sendReply()" class="bg-primary text-white px-6 py-2 rounded-lg hover:bg-primary/90 text-sm font-semibold">
                      Invia
                    </button>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </main>
  </div>

  <script>
    let currentConversation = null;
    let aiEnabled = false;

    // Toggle AI
    function toggleAI(enabled) {
      aiEnabled = enabled;
      const statusEl = document.getElementById('aiStatus');
      statusEl.textContent = enabled ? 'ON' : 'OFF';
      statusEl.className = enabled ? 'text-xs font-semibold text-green-600' : 'text-xs font-semibold text-gray-500';

      if (enabled) {
        alert('✅ AI Auto-Risposta ATTIVATA\n\nSmileAgent risponderà automaticamente alle email Gmail quando rileva richieste esplicite o domande dirette.');
      }
    }

    // Connetti Gmail
    async function connectGmail() {
      try {
        const res = await fetch('/api/messaggi/gmail/auth-url');
        const data = await res.json();

        if (data.success) {
          window.location.href = data.authUrl;
        } else {
          alert('Errore: ' + data.error);
        }
      } catch (error) {
        console.error(error);
        alert('Errore connessione Gmail');
      }
    }

    // Filter messages
    function filterMessages(channel) {
      const allBtns = document.querySelectorAll('.filter-btn');
      allBtns.forEach(btn => {
        btn.classList.remove('active', 'bg-primary', 'text-white');
        btn.classList.add('text-gray-700', 'dark:text-gray-300', 'hover:bg-gray-100', 'dark:hover:bg-gray-800');
      });

      event.target.classList.add('active', 'bg-primary', 'text-white');
      event.target.classList.remove('text-gray-700', 'dark:text-gray-300', 'hover:bg-gray-100', 'dark:hover:bg-gray-800');

      const messages = document.querySelectorAll('.message-item');
      messages.forEach(msg => {
        if (channel === 'all') {
          msg.style.display = 'block';
        } else {
          msg.style.display = msg.classList.contains(channel + '-msg') ? 'block' : 'none';
        }
      });
    }

    // Open conversation
    function openConversation(channel, id) {
      currentConversation = { channel, id };

      document.getElementById('conversationPlaceholder').classList.add('hidden');
      document.getElementById('conversationView').classList.remove('hidden');

      // Mock data
      const mockConversations = {
        'fb_demo_001': {
          name: 'Laura Verdi',
          channel: '📘 Facebook (Demo)',
          avatar: 'FB',
          avatarBg: 'bg-blue-600',
          messages: [
            { from: 'user', text: 'Quanto costa l\'apparecchio invisibile? Mio figlio ne avrebbe bisogno...', time: '5 ore fa' },
            { from: 'ai', text: 'Buongiorno Laura! Per l\'apparecchio invisibile i costi variano in base al piano di trattamento. La invito a contattarci al numero dello studio per una consulenza gratuita personalizzata. Cordiali saluti, SmileAgent', time: '4 ore fa' }
          ]
        },
        'ig_demo_001': {
          name: '@dentista_fan',
          channel: '📷 Instagram (Demo)',
          avatar: 'IG',
          avatarBg: 'bg-gradient-to-br from-purple-600 to-pink-600',
          messages: [
            { from: 'user', text: 'Complimenti per il post sullo sbiancamento! Fate anche consulenze gratuite?', time: '1 giorno fa' }
          ]
        },
        'fb_demo_002': {
          name: 'Marco Bianchi',
          channel: '📘 Facebook (Demo)',
          avatar: 'FB',
          avatarBg: 'bg-blue-600',
          messages: [
            { from: 'user', text: 'Salve, vorrei prenotare una pulizia dentale. Quali sono i vostri orari?', time: '2 giorni fa' }
          ]
        }
      };

      const conv = mockConversations[id];
      if (!conv) return;

      document.getElementById('convAvatar').className = `w-12 h-12 ${conv.avatarBg} rounded-full flex items-center justify-center`;
      document.getElementById('convAvatar').innerHTML = `<span class="text-white font-bold">${conv.avatar}</span>`;
      document.getElementById('convName').textContent = conv.name;
      document.getElementById('convChannel').textContent = conv.channel;

      const messagesHTML = conv.messages.map(msg => {
        if (msg.from === 'user') {
          return `
            <div class="flex justify-start">
              <div class="bg-gray-100 dark:bg-gray-800 rounded-lg px-3 py-2 max-w-xs">
                <p class="text-sm text-gray-900 dark:text-white">${msg.text}</p>
                <p class="text-xs text-gray-500 mt-1">${msg.time}</p>
              </div>
            </div>
          `;
        } else {
          return `
            <div class="flex justify-end">
              <div class="bg-primary/10 rounded-lg px-3 py-2 max-w-xs">
                <p class="text-sm text-gray-900 dark:text-white">${msg.text}</p>
                <p class="text-xs text-gray-500 mt-1">${msg.time}</p>
              </div>
            </div>
          `;
        }
      }).join('');

      document.getElementById('convMessages').innerHTML = messagesHTML;
    }

    // Generate AI Reply
    async function generateAIReply() {
      if (!currentConversation) return;

      const replyBox = document.getElementById('replyText');
      replyBox.value = '⏳ Generazione risposta AI...';
      replyBox.disabled = true;

      // Simulate AI generation
      setTimeout(() => {
        replyBox.value = 'Gentile paziente,\n\nGrazie per averci contattato! Saremo lieti di aiutarLa. La invitiamo a chiamarci al numero dello studio per fissare un appuntamento o ricevere maggiori informazioni.\n\nCordiali saluti,\nSmileAgent - Assistente Virtuale';
        replyBox.disabled = false;
      }, 1500);
    }

    // Send Reply
    function sendReply() {
      const text = document.getElementById('replyText').value;
      if (!text.trim()) {
        alert('Scrivi un messaggio prima di inviare');
        return;
      }

      alert('✅ Risposta inviata!\n\n(Funzione demo - integrazione completa con Gmail/Facebook/Instagram in arrivo)');
      document.getElementById('replyText').value = '';
    }

    // Load Gmail emails if connected
    async function loadGmailEmails() {
      try {
        const res = await fetch('/api/messaggi/gmail/sync', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ maxResults: 50 })
        });

        const data = await res.json();

        if (data.success && data.emails && data.emails.length > 0) {
          // Hide Gmail connect banner
          document.getElementById('gmailConnect').style.display = 'none';

          // Remove demo messages (keep them or remove based on preference)
          // document.querySelectorAll('.facebook-msg, .instagram-msg').forEach(el => el.remove());

          // Add real Gmail emails
          const messagesList = document.getElementById('messagesList');

          data.emails.forEach(email => {
            const emailDiv = document.createElement('div');
            emailDiv.className = 'message-item gmail-msg bg-white dark:bg-gray-900 rounded-lg border border-gray-200 dark:border-gray-700 p-4 hover:shadow-md transition-shadow cursor-pointer';
            emailDiv.onclick = () => openConversation('gmail', email.threadId);

            emailDiv.innerHTML = `
              <div class="flex items-start gap-3">
                <div class="w-10 h-10 bg-red-600 rounded-full flex items-center justify-center flex-shrink-0">
                  <span class="text-white font-bold">@</span>
                </div>
                <div class="flex-1 min-w-0">
                  <div class="flex items-center justify-between mb-1">
                    <div class="flex items-center gap-2">
                      <span class="font-semibold text-gray-900 dark:text-white">${email.from_name || email.from_email}</span>
                      <span class="text-xs bg-red-100 dark:bg-red-900 text-red-700 dark:text-red-300 px-2 py-0.5 rounded">📧 Gmail</span>
                      ${email.ai_replied ? '<span class="text-xs bg-green-100 dark:bg-green-900 text-green-700 dark:text-green-300 px-2 py-0.5 rounded">✓ AI</span>' : ''}
                    </div>
                    <span class="text-xs text-gray-500">${new Date(email.last_message_date).toLocaleString('it-IT', { month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit' })}</span>
                  </div>
                  <p class="text-sm font-medium text-gray-900 dark:text-white mb-1">${email.subject || '(Nessun oggetto)'}</p>
                  <p class="text-sm text-gray-600 dark:text-gray-400 truncate">${email.snippet || ''}</p>
                </div>
              </div>
            `;

            messagesList.appendChild(emailDiv);
          });

          console.log(`✅ Caricate ${data.emails.length} email Gmail vere!`);
        }
      } catch (error) {
        console.error('Error loading Gmail emails:', error);
      }
    }

    // Initialize filter buttons styling
    document.addEventListener('DOMContentLoaded', () => {
      const firstBtn = document.querySelector('.filter-btn.active');
      if (firstBtn) {
        firstBtn.classList.add('bg-primary', 'text-white');
      }

      document.querySelectorAll('.filter-btn:not(.active)').forEach(btn => {
        btn.classList.add('text-gray-700', 'dark:text-gray-300', 'hover:bg-gray-100', 'dark:hover:bg-gray-800');
      });

      // Check if Gmail was just connected
      const urlParams = new URLSearchParams(window.location.search);
      if (urlParams.get('success') === 'gmail_connected') {
        // Load Gmail emails after successful connection
        setTimeout(() => loadGmailEmails(), 1000);
      }

      // Also try to load Gmail emails on every page load (if already connected)
      loadGmailEmails();
    });
  </script>
</body>
</html>
